version: 2

build:
  os: ubuntu-22.04
  tools:
    python: {}    # specify python as build tool
  commands:
    - pip install -r docs/requirements.txt   # optional, agar dependencies ho
    - sphinx-build -b html docs/ docs/_build/html

sphinx:
  configuration: docs/conf.py
